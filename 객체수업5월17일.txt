1.레퍼런스 변수
2.function overloading
3.디폴트값을 사용한 함수입력반자들 함수 매개변수호출안해도 디폴트값 줄수있다.
주의)중간에 디폴트값 들어가있으면 안된다.
더블포인터 **?

포인터는 메모리공간을 만들어 실체가 있다.

레페런스는 메모리공간을 만들지 않아 실체가 없다.
(알려주는 정보가 필수) 닉네임이다. 관계형성만 값을 넣어주는것이 아니다.

레퍼런스 사용(멤버 초기화 리스트)
1.멤버 변수 레퍼런스 사용 할때는 생성자함수에서 초기화만 해주며 된다.
ex) Screen& screen / screen(*screen)
2.함수 매개변수 레퍼런스 변수 사용(함수 호출할때 결정)
3.함수 반환형 레퍼런스 자료형(자기자신 객체 호출할때) chaining기법
return *this;
ex)player mine; mine.shoot(10).shoot(20);

c컴파일은 오버로딩 불가능