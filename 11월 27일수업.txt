static 함수 바디에 const는 사용 불가능 멤버 변수를 바꾸지 않는다는 뜻

static함수이기때문에 this포인터가 안됨 멤버 변수 자체가 없다는 뜻

해슁 문자열을 숫자로 바꾸는것 (해쉬 테이블을 이용하여 찾는다) O(1) 갖는다
가장 빨리 찾는 방법은 ? 해쉬 테이블 

자료구조 map
단점 : 메모리 사용이 크기때문에 tree를 사용

list는 삭제나 삽입시 문제가 있다. 그래서 vector를 사용

이름 Find
절대경로 따라서 Find

for each문 사용시 포인터를 가져오는게 아닐때는 for extend문
auto& 레퍼런스 붙여서 사용 오버헤드 안생긴다.

람다함수는 함수 ( ) 안에 조건이 들어간다.

